(function(){
function proStyle(){if($("#proStyle"))return;document.head.insertAdjacentHTML("beforeend",`<style id="proStyle">.proGrid{display:grid;grid-template-columns:repeat(auto-fit,minmax(320px,1fr));gap:14px}.kvBox{display:grid;gap:8px;margin-top:10px}.kv{display:flex;justify-content:space-between;gap:10px;border:1px solid var(--line);border-radius:7px;background:#f8fafc;padding:9px 10px}.kv span{color:var(--muted)}.kv strong{text-align:right;overflow-wrap:anywhere}.checkList{margin:0;padding-left:20px}.checkList li{margin:7px 0}</style>`)}
function addPro(){let P=window.kespPro;if(!P||$("#pro"))return;proStyle();$("#tabs")?.insertAdjacentHTML("beforeend",`<button class="tab" data-tab="pro">Professional</button>`);$("#panels")?.insertAdjacentHTML("beforeend",`<section class="panel" id="pro"><div class="proGrid"><section class="card"><h2>Release & Diagnostics</h2><p class="muted">Read-only checks; no flash writes.</p><div class="quick"><button id="runPreflight" type="button">Run preflight</button><button id="exportDiag" type="button">Export diagnostics</button><a class="btn secondary" href="${apiUrl("/backup")}">Download backup first</a></div><div id="proOut" class="kvBox"></div></section><section class="card"><h2>Board Profile</h2><form id="boardForm"><select id="boardProfile">${Object.keys(P.profs).map(k=>`<option value="${k}">${esc(P.profs[k].label)}</option>`).join("")}</select><button>Apply profile</button></form><div id="pinGuide" class="kvBox"></div><pre id="profileOut"></pre></section></div><div class="proGrid"><section class="card"><h2>Update Preflight</h2><ul class="checkList"><li>Download backup first</li><li>Run preflight and confirm heap/LittleFS health</li><li>Compile and package with tools/release.sh</li><li>Upload firmware by serial unless OTA is proven safe for IRAM</li><li>Upload LittleFS assets with tools/upload-assets.sh</li></ul></section><section class="card"><h2>Install Checklist</h2><ul class="checkList"><li>Set web key</li><li>Set Wi-Fi and NTP timezone</li><li>Select board profile</li><li>Add relays with safe boot state</li><li>Test pulses without dangerous loads</li><li>Create backup/profile after validation</li></ul></section></div></section>`);P.paintProfile();P.loadProfile()}
document.addEventListener("click",async e=>{let P=window.kespPro;if(!P)return;if(e.target.id=="runPreflight"){await P.runPreflight();return}if(e.target.id=="exportDiag"){let t=await P.diagText();P.dl("kernelesp-diagnostic-"+new Date().toISOString().replace(/[:.]/g,"-")+".txt",t);return}});
document.addEventListener("submit",async e=>{if(e.target.id=="boardForm"){e.preventDefault();await window.kespPro.saveProfile()}});
document.addEventListener("change",e=>{if(e.target.id=="boardProfile")window.kespPro.paintProfile(e.target.value)});
addPro();
import("/app12.js?v=4");
})();
